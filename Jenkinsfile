pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                
                sh "cd /usr/bin/node"
                sh 'npm --version'
            }
        }
    }
}
